version: '3.8'

services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - ./scripts:/tests/scripts
    # environment:
    #   - GOPATH=/go
    command: /usr/local/bin/entrypoint.sh

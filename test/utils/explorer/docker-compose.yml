services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - ./scripts/:/workspace
    environment:
      - GOPATH=/go
    command: /usr/local/bin/entrypoint.sh

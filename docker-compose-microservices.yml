# Define a common base configuration for teranode nodes
x-teranode-base:
  &teranode-base
  image: teranode:latest
  depends_on:
    aerospike:
      condition: service_started
    postgres:
      condition: service_started
    kafka-shared:
      condition: service_started
  networks:
    - teranode-network
  volumes:
    - ./data:/app/data
    - ./settings_local.conf:/app/settings_local.conf
    - ./data/teranode/txstore:/app/data/txstore
    - ./data/teranode/subtreestore:/app/data/subtreestore
    - ./data/teranode/blockstore:/app/data/blockstore
  expose:
    - 8081-8093
    - 9095-9096
    - 9292
    - 3005
  environment:
    SETTINGS_CONTEXT: ${SETTINGS_CONTEXT_1:-docker.m}
    logLevel: "DEBUG"

networks:
  teranode-network:
    name: my-teranode-network

services:
  teranode-builder:
    image: teranode:latest
    build:
      context: .
      dockerfile: local.Dockerfile
      args:
        BASE_IMG: 434394763103.dkr.ecr.eu-north-1.amazonaws.com/ubsv:base-build-db1a6f0
        RUN_IMG: 434394763103.dkr.ecr.eu-north-1.amazonaws.com/ubsv:base-run-db1a6f0
    networks:
      - teranode-network
    entrypoint: ["pwd"]

  postgres:
    container_name: postgres
    image: postgres:latest
    networks:
      - teranode-network
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: really_strong_password_change_me
      POSTGRES_DB: postgres
    ports:
      - 5432:5432
    expose:
      - 5432
    volumes:
      - ./scripts/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./data/postgres:/var/lib/postgresql/data
    restart: on-failure
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  kafka-shared:
    container_name: kafka-shared
    image: redpandadata/redpanda:latest
    mem_limit: 256m
    networks:
      - teranode-network
    command:
      - redpanda start
      - --smp 1
      - --overprovisioned
      - --node-id 0
      - --kafka-addr PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr PLAINTEXT://kafka-shared:9092
      - --pandaproxy-addr 0.0.0.0:9093
      - --advertise-pandaproxy-addr localhost:9093
    ports:
      - 8081
      - 9092:9092
      - 9093:9093

  kafka-console-shared:
    container_name: kafka-console-shared
    image: docker.redpanda.com/redpandadata/console:latest
    networks:
      - teranode-network
    restart: on-failure
    entrypoint: /bin/sh
    command: -c "echo \"$$CONSOLE_CONFIG_FILE\" > /tmp/config.yml; /app/console"
    environment:
      CONFIG_FILEPATH: /tmp/config.yml
      CONSOLE_CONFIG_FILE: |
        kafka:
          brokers: ["kafka-shared:9092"]
          schemaRegistry:
            enabled: true
            urls: ["http://kafka-shared:8081"]
    ports:
      - 8080
    depends_on:
      - kafka-shared

  aerospike:
    container_name: aerospike
    image: aerospike:ce-6.4.0.7_2
    command: --config-file /etc/aerospike.conf
    networks:
      - teranode-network
    ports:
      - 3000:3000
    expose:
      - 3000
    restart: on-failure
    volumes:
      - ./deploy/dev/aerospike/aerospike.conf:/etc/aerospike.conf
      - ./data/aerospike/data:/opt/aerospike/data

  aerospike-exporter:
    image: aerospike/aerospike-prometheus-exporter:latest
    container_name: aerospike-exporter
    environment:
      AS_HOST: aerospike
      AS_PORT: 3000
      AGENT_BIND_PORT: 10145
      METRIC_LABELS: "type='development',source='aerospike'"
    network_mode: bridge

  teranode-blockchain:
    <<: *teranode-base
    container_name: teranode-blockchain
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "1", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-blockchain=1"]
    ports:
      - 4040
      - 8082
      - 8087
      - 9091
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8087"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  teranode-asset:
    <<: *teranode-base
    container_name: teranode-asset
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-asset=1"]
    ports:
      - 4040
      - 8090
      - 8091
      - 9091
    depends_on:
      teranode-blockchain:
        condition: service_healthy
      teranode-p2p:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8091"]
      # test: ["CMD-SHELL", "curl", "-f", "http://localhost:8090"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  teranode-miner:
    <<: *teranode-base
    container_name: teranode-miner
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run"]
    ports:
      - 4040
      - 8089
      - 8092
      - 8099
      - 9091
    depends_on:
      teranode-blockchain:
        condition: service_healthy
      teranode-blockvalidation:
        condition: service_healthy
      teranode-blockassembly:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8092"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s
  
  teranode-coinbase:
    <<: *teranode-base
    container_name: teranode-coinbase
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-coinbase=1", "-blockpersister=0"]
    ports:
      - 4040
      - 8093
      - 9091
    depends_on:
      teranode-asset:
        condition: service_healthy
      teranode-miner:
        condition: service_healthy
      teranode-blockchain:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8093"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  teranode-p2p:
    <<: *teranode-base
    container_name: teranode-p2p
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-p2p=1"]
    ports:
      - 4040
      - 9091
      - 9905
      - 9906
    depends_on:
      teranode-blockchain:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "9905"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  teranode-rpc:
    <<: *teranode-base
    container_name: teranode-rpc
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-rpc=1"]
    ports:
      - 4040
      - 9091
      - 9292
    depends_on:
      teranode-blockchain:
        condition: service_healthy

  teranode-subtreevalidation:
    <<: *teranode-base
    container_name: teranode-subtreevalidation
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-subtreevalidation=1"]
    ports:
      - 4040
      - 8086
      - 9091
    depends_on:
      teranode-blockchain:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8086"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  teranode-blockvalidation:
    <<: *teranode-base
    container_name: teranode-blockvalidation
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-blockvalidation=1"]
    ports:
      - 4040
      - 8088
      - 8188
      - 9091
    depends_on:
      teranode-blockchain:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8088"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  teranode-blockassembly:
    <<: *teranode-base
    container_name: teranode-blockassembly
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-blockassembly=1"]
    ports:
      - 4040
      - 8085
      - 8285
      - 9091
    depends_on:
      teranode-blockchain:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8085"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s
  
  teranode-blockpersister:
    <<: *teranode-base
    container_name: teranode-blockpersister
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-blockpersister=1"]
    ports:
      - 4040
    depends_on:
      teranode-blockchain:
        condition: service_healthy
      teranode-blockvalidation:
        condition: service_healthy
      teranode-blockassembly:
        condition: service_healthy

  teranode-propagation:
    <<: *teranode-base
    container_name: teranode-propagation
    entrypoint: ["/app/wait.sh", "aerospike", "3000", "2", "--", "/app/wait.sh", "postgres", "5432", "0", "--", "/app/wait.sh", "kafka-shared", "9092", "0", "--", "/app/teranode.run", "-propagation=1"]
    ports:
      - 4040
      - 8833
      - 8084
      - 8384
      - 9091
    depends_on:
      teranode-blockchain:
        condition: service_healthy
      teranode-blockvalidation:
        condition: service_healthy
      teranode-blockassembly:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "netstat", "-ltn", "|", "grep", "-c", "8084"]
      interval: 5s
      timeout: 30s
      retries: 10
      start_period: 7s

  prometheus:
    container_name: prometheus
    image: prom/prometheus:v2.44.0
    mem_limit: 64m
    ports:
      - 9090
    networks:
      - teranode-network
    volumes:
      - ./deploy/dev/prometheus/prometheus-microservices.yml:/etc/prometheus/prometheus.yml

  grafana:
    container_name: grafana
    image: grafana/grafana:latest
    ports:
      - 3005:3000
    networks:
      - teranode-network
    volumes:
      - ./deploy/dev/grafana/datasources/main-microservices.yaml:/etc/grafana/provisioning/datasources/main.yaml
      - ./deploy/dev/grafana/dashboards/main.yaml:/etc/grafana/provisioning/dashboards/main.yaml
      - ./deploy/dev/grafana/dashboards:/var/lib/grafana/dashboards
      - ./deploy/dev/grafana/grafana.db:/var/lib/grafana
    depends_on:
      - prometheus
